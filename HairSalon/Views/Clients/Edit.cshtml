@{
  Layout = "_Layout";
}

@model Organization.Models.Client

<h2>Editing client <strong>@Html.DisplayFor(model => model.Name)</strong></h2>

@using (Html.BeginForm())
{
  <div class="form-group">
    @Html.HiddenFor(model => model.ClientId)
  </div>
  <div class="form-group">
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)
  </div>
  <div class="form-group">
    @Html.LabelFor(model => model.Employee)
    @Html.DropDownList("EmployeeId")
  </div>
    <input class="btn btn-light form-group" type="submit" value="Save" />
}
<p class="btn btn-light">@Html.ActionLink("Back to list", "Index")</p>