@{
  Layout = "_Layout";
}

@model Organization.Models.Client
@* The line above is a model directive - tells our view what type of data will be passed into the view from the controller route *@

<h4>Add a new task</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)

  @Html.LabelFor(model => model.Employee)
  @Html.DropDownList("EmployeeId")
  <input type="submit" value="Add new task" />
}
<p>@Html.ActionLink("Show all clients", "Index")</p>

@* By default, BeginForm() sends an HTTP POST request to the current URL. This form will send a POST request to the Create() route. In HTML, it would look like this: <form action="/Clients/Create" method="post"> *@
