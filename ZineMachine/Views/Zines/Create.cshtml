@{
  Layout = "_Layout";
}

@model Organization.Models.Zine

<h2>Add a new task</h2>
<hr>
@using (Html.BeginForm())
{
  <div class="form-group">
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name, new {@Class = "form-control"})
  </div>
  <div class="form-group">
    @Html.LabelFor(model => model.User)
    @Html.DropDownList("UserId", ViewBag.UserId, "Select User", new { @class = "form-control form-control-sm" })
  </div>
  <input class="btn btn-light form-group" type="submit" value="Add zine" />
}
<p class="btn btn-light">@Html.ActionLink("Cancel", "Index")</p>
