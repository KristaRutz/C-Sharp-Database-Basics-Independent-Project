CREATE DATABASE `zine_machine`;
USE zine_machine;
CREATE TABLE `users` (
  `UserId` int(11) NOT NULL AUTO_INCREMENT,
  `Name` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`UserId`)
);
CREATE TABLE `zines` (
  `ZineId` int(11) NOT NULL AUTO_INCREMENT,
  `Name` varchar(255) DEFAULT NULL,
  `UserId` int(11) NOT NULL DEFAULT '0',
  PRIMARY KEY (`ZineId`)
);
CREATE TABLE `posts` (
  `PostId` int(11) NOT NULL AUTO_INCREMENT,
  `ZineId` int(11) NOT NULL DEFAULT '0',
  `TextContent` varchar(255) DEFAULT NULL,
  `ContentSource` varchar(255) DEFAULT NULL,
  `PostType` varchar(45) DEFAULT NULL,
  `TimeCreated` datetime NOT NULL,
  PRIMARY KEY (`PostId`)
);
